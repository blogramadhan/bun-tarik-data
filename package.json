{
  "name": "tarik-data",
  "private": true,
  "scripts": {
    "validate-env": "bun run src/validate-env.ts",
    "upload-r2": "bun run src/sendR2.ts",
    "dev": "bun --watch src/sendR2.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.832.0",
    "duckdb": "^1.3.0"
  }
}